# DIAGNOSTIC BUILD -- minimal. Pointing/mouse deliberately OFF so a stuck
# position cannot move the cursor or click while testing.

CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000

CONFIG_ZMK_USB_LOGGING=y
